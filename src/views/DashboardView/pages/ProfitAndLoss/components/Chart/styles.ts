import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
  },

  hint: {
    position: 'relative',
    background: 'black',
    opacity: '0.9',
    borderRadius: '8px',
    padding: '8px',
    zIndex: 2
  }
}, { name: 'finance-group-chart' });
