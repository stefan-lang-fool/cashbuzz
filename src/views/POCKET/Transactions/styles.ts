import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
    width: 'calc(100% + 48px)',
    margin: '0 -24px'
  },

  hidden: {
    display: 'none'
  }
}, { name: 'transactions' });
